<footer class="footer text-center">
  <div class="wrapper">
    <div class="fs-row">
      <div class="fs-cell fs-all-full">
        <ul class="inline-list">
          {% for link in linklists.footer.links %}
          <li><a class="footer-link" href="{{ link.url }}">{{ link.title }}</a></li>
          {% endfor %}
          <li>© {{ shop.name }} {{'now' | date: '%Y' }}</li>
      </div>
    </div>
  </div>
</footer>

{% schema %}
  {
    "name": "Footer",
    "settings": [
      {
        "type": "link_list",
        "id": "footer_menu",
        "label": "Footer Menu"
      }
    ]
  }
{% endschema %}